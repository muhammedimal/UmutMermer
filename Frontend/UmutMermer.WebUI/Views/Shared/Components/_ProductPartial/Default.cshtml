@model List<ResultProductVM>

<section class="portfolio">
    <div class="container">
       

        <div class="row shuffle-wrapper portfolio-gallery">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-6 mb-4 shuffle-item" data-groups="[&quot;design&quot;,&quot;illustration&quot;]">
                    <div class="position-relative inner-box">
                        <div class="image position-relative ">
                            @foreach (var image in item.ProductImage)
                            {
                                <img src="http://localhost:5112/@image.Path" alt="portfolio-image" class="img-fluid w-100 d-block" width="109" height="109">
                                @*ressim ekleme öğren*@
                            }
                            <div class="overlay-box">
                                <div class="overlay-inner">
                                    <a class="overlay-content" href="portfolio-single.html">
                                        <h5 class="mb-0">@item.Name</h5>
                                        <p>@item.Name</p>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</section>
@model List<GetCategoryDto>
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@

@*<section class="portfolio ">
    <div class="container">
        <div class="row mb-5">
            <div class="col-10">
                @foreach (var item in Model)
                {

                    @*<li class="nav-item" role="presentation">
                        <button class="nav-link" onclick="getContentDetail('@item.Id')">@item.Name</button>
                    </li>*@

@*
            </div>
        </div>
    </div>
</section>*@


<div class="container">
    <div class="row mb-5">
        <div class="col-10">
            <div class="btn-group btn-group-toggle " data-toggle="buttons">
                @foreach (var item in Model)
                {

                    <label class="btn active ">
                        <input type="radio" name="shuffle-filter" value="all" checked="checked" onclick="getContentDetail('@item.Id')">@item.Name
                    </label>

                }
            </div>
        </div>
    </div>
</div>